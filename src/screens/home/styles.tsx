import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {},
  titleContainer: {
    alignItems: 'center',
    width: '100%',
    marginBottom: 20,
    paddingHorizontal: 40,
  },
  titleText: {
    fontSize: 20,
  },
});
